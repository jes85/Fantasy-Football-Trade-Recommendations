/**
 * A TradeInputStorer stores trade input data to an arbitrary destination.
 *
 * "TradeInput" is the input data required to run the trade algorithm.
 */
class TradeInputStorer {

  /**
   * Save the TradeInput data to in arbitrary destination.
   * @param {TradeInput} tradeInput
   */
  saveTradeInput(tradeInput) {
  }
  /**
   * Save the given league to an arbitrary destination.
   * @param {League} league
   */
  saveLeague(league) {
  }

  /**
   * Save the player projections to an arbitrary destination
   * @param {Map<String, float>} playerProjections
   */
  savePlayerProjections(playerProjections) {

  }
}

export default TradeInputStorer;